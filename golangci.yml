linters-settings:
  golint:
    enabled: true
  govet:
    enabled: true
  staticcheck:
    enabled: true

linters:
  disable-all: true
  enable:
    - revive
    - govet
    - staticcheck

run:
  paths:
    - ./...